launcher: local

modules: txpipe


stages:
    - name: PZPDFMLZ
      nprocess: 1
    - name: TXSelector
      nprocess: 1
    - name: TXDiagnosticMaps
      nprocess: 1
    - name: TXPhotozStack
      nprocess: 1
    - name: TXRandomCat
      nprocess: 1
    - name: TXSysTests
      nprocess: 1

output_dir: ./outputs

config: test/config.yml

inputs:
    # See README for paths to download these files
    shear_catalog: ./shear_catalog.fits
    photometry_catalog: ./photometry_catalog.hdf
    photoz_trained_model: ./photoz_trained_model.npy

resume: True
log_dir: ./test/logs
pipeline_log: log.txt
